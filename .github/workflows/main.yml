name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Greeting
        run: echo "Hello, world!"

      - name: Another greeting
        run: echo "Hello, world!"
   
  log-variable:
    runs-on: ubuntu-latest
    outputs:
      failures: ${{ steps.set-variable.outputs.fail_count }}
      report: ${{ steps.set-report.outputs.fail_report }}

    steps:
    
      - uses: actions/checkout@v2
      
      - name: Set variable
        id: set-variable
        shell: bash
        run: |
          chmod +x "${GITHUB_WORKSPACE}/.github/workflows/scripts/cypress-fail-report-transformer.sh"
          bash "${GITHUB_WORKSPACE}/.github//workflows/scripts/cypress-fail-report-transformer.sh"
      
      - name: Set full report
        id: set-report
        run: |
          full_report=$(cat << EOF
            ${{ steps.set-variable.outputs.fail_report }}
          EOF
          )

          full_report=$(echo ${full_report//$'\n'/'%0A'} | sed 's/"//g')
          echo "::set-output name=fail_report::"${full_report}""
          

          echo "slack hook"
          echo "${{ secrets.SLACK_WEBHOOK_URL }}"
          echo -e "$full_report"
          
  slack-reporter:
    runs-on: ubuntu-latest
    needs:
      - build
      - log-variable
    
    steps:
    
      - uses: actions/checkout@v2
      
      - name: Check-variable
        id: check-variable
        run: |
          echo '${{ needs.log-variable.outputs.report }}'
          echo "${{ needs.log-variable.outputs.failures }}"
      
      - name: Send custom JSON data to Slack workflow
        id: slack
        uses: slackapi/slack-github-action@v1.18.0
        with:
          # For posting a rich message using Block Kit
          payload: |
            {
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": ":warning: *Oops!* Found ${{ needs.log-variable.outputs.failures }} integration test fail(s) on Cypress. Huehue. :pleading_face:"
                  }
                },
                {
                  "type": "divider"
                },
                {
                  "type": "context",
                  "elements": [
                    {
                      "type": "mrkdwn",
                      "text": ":technologist::skin-tone-2: *Commit by* ${{ github.event.pull_request.user.login }} from branch *${{ github.event.pull_request.base.ref }}*"
                    }
                  ]
                }
              ],
              "attachments": [
                {
                  "color": "#FF0000",
                  "blocks": [
                    {
                      "type": "section",
                      "text": {
                        "type": "mrkdwn",
                        "text": "Please check this out. :pray:"
                      }
                    },
                    {
                      "type": "context",
                      "elements": [
                        {
                          "type": "mrkdwn",
                          "text": "${{ needs.log-variable.outputs.report }}"
                        }
                      ]
                    },
                    {
                      "type": "divider"
                    },
                    {
                      "type": "context",
                      "elements": [
                        {
                          "type": "mrkdwn",
                          "text": "more info: <${{ github.server_url }}/${{ github.repository }}/commit/${{ github.event.pull_request.head.sha }}|from commit> | <${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|from workflow>"
                        }
                      ]
                    }
                  ]
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
          SLACK_WEBHOOK_TYPE: INCOMING_WEBHOOK
